# Logistic-Dashboard
The Dashboard, it contains mutiple graphs, KPI's which indicates entire visualizations in single report. In addition to, it make easy understanding instead of raw data.
Instead of calculating through data, here these visuals can help understand even more better. 
